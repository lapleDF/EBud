import {StyleSheet} from 'react-native';

export const GuessTheWordPlaceholderStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-between',
    gap: 20,
    marginVertical: 20,
    flex: 1,
  },
});
