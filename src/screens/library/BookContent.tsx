import React from 'react';
import {CSLayout, CSText} from '../../components/core';

const BookContent = () => {
  return (
    <CSLayout>
      <CSText>BookContent</CSText>
    </CSLayout>
  );
};

export default BookContent;
