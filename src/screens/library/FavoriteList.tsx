import React from 'react';
import CSContainer from '../../components/core/CSContainer';
import CSText from '../../components/core/CSText';

const FavoriteList = () => {
  return (
    <CSContainer>
      <CSText>FavoriteList</CSText>
    </CSContainer>
  );
};

export default FavoriteList;
