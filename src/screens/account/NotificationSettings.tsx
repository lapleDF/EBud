import React from 'react';
import {CSLayout, CSText} from '../../components/core';

const NotificationSettings = () => {
  return (
    <CSLayout>
      <CSText>NotificationSettings</CSText>
    </CSLayout>
  );
};

export default NotificationSettings;
