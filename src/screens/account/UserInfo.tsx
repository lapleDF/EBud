import React from 'react';
import CSContainer from '../../components/core/CSContainer';
import CSText from '../../components/core/CSText';

const UserInfo = () => {
  return (
    <CSContainer>
      <CSText>UserInfo</CSText>
    </CSContainer>
  );
};

export default UserInfo;
